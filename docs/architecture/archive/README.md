# ADR Archive

Fossilized architecture decisions moved here by `fossilize.sh` when active ADR exceeds 25 non-empty lines (user confirmation required).

Format: `YYYY-MM-fossilize-<domain>-<timestamp>.md` or `YYYY-MM-<domain>-<slug>.md`

Do not edit archived files. Summaries may be squashed into `ADR.md` CORE RULES in future releases.

One fossilize run archives up to 2 oldest RECENT entries (gradual, not cliff).
